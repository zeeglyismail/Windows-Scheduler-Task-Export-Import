# Windows-Task-Scheduler-Export-Import
As the default ui give us ability to export one task at a time this script will export all at once without the system level task such as microsoft*  
Run the powershell as administrator  
.\import_task.ps1  
.\export_task.ps1  
Dont forget to check the PATH  
